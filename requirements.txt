# Core (app imports via PYTHONPATH=/app/src on Railway; editable install locally)
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9
alembic>=1.13
pydantic>=2.0
pydantic-settings>=2.0
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
click>=8.1
tenacity>=8.2
python-dotenv>=1.0

# Web dashboard
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1

# Analysis / quant
pandas>=2.0
numpy>=1.26
scipy>=1.11

# Scraping extras (not needed by the web app at runtime)
# chrono24>=0.3
# curl_cffi>=0.7
# apscheduler>=3.10,<4.0
# fake-useragent>=1.4
